plugins {
    id 'application'
}
application {
    mainClass = 'Assign2'
}
dependencies {
    // No external dependencies
}
tasks.named("run") {
    if (project.hasProperty("runArgs")) {
        args project.runArgs.split(" ")
    }
}
